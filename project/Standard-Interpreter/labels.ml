(* labels.ml *)
open Concrete_To_Abstract_Syntax
(* labels *)
let at               = at
let after            = after      (* command exit labels     *)
let incom            = incom      (* label in command    *)
let number_of_labels = number_of_labels
let entry            = entry      (* program entry label     *)
let exit             = exit       (* program exit label      *)
let print_label      = print_label
let string_of_label  = string_of_label 
